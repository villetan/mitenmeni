require 'rails_helper'

#RSpec.describe UsersController, :type => :controller do
    #describe  "PACTH edit" do
      #it "cannot edit other users" do
        #FactoryGirl.create(:user)
        #params={:user=>{:username=>"lol", :old_password=>"Pekka1", :password=>"kekek", :password_confirmation=>"kekek"}}
        #patch :update,:id=> User.first.id, :user=>{:username=>"lol", :old_password=>"Pekka1", :password=>"kekek", :password_confirmation=>"kekek"}
        #expect(User.first.username).to eq("lol")
       # expect(User.authenticate?("lol", "Pekka1")).to be(true)
      #  expect(User.authenticate?("lol", "kekek")).to be(true)
     # end

#    end
#end