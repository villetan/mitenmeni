<p id="notice"><%= notice %></p>

<p>
  <strong>Score:</strong>
  <%= @rating.score %>
</p>

<p>
  <strong>User:</strong>
  <% if @rating.user_id %>
      <%= link_to @rating.user.username, @rating.user  %>
  <% else %>
    anon
  <% end %>

</p>
<p>
  <%= link_to @rating.get_place.name, places_path+"/"+@rating.get_place.place_id%>
</p>

<%= link_to 'Back', :back %>
